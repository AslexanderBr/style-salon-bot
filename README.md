# Style Salon Telegram Bot (Legacy Version)

> **Важный комментарий**:  
> Перед обновлением до новой версии (GitHub-релиз) была проведена критически важная миграция проекта, включающая:  
> 
> ### 🔒 Безопасность токена
> 1. Создан `.env`-файл для хранения конфиденциальных данных:
>    ```env
>    BOT_TOKEN=your_actual_token_here
>    ```
> 2. Токен удалён из `index.js` (теперь используется `process.env.BOT_TOKEN`).
> 3. Установлен пакет `dotenv` для работы с переменными окружения:
>    ```bash
>    npm install dotenv
>    ```
> 4. Добавлен `.gitignore` для исключения секретных файлов:
>    ```gitignore
>    .env
>    node_modules/
>    bot.log
>    ```
>
> ### 🚀 Миграция на GitHub
> - Репозиторий инициализирован через:
>   ```bash
>   git init
>   git add .
>   git commit -m "Legacy version before major update"
>   ```
> - Проект загружен на GitHub с полной историей:
>   ```bash
>   git remote add origin https://github.com/AslexanderBr/style-salon-bot.git
>   git branch -M main
>   git push -u origin main
>   ```
>
> ### ⚠️ Если токен был раскрыт
> 1. Удалён из истории Git:
>    ```bash
>    git filter-branch --force --index-filter 'git rm --cached --ignore-unmatch .env' --prune-empty --tag-name-filter cat -- --all
>    ```
> 2. Токен заменён в @BotFather.

---

## Исходное описание (до миграции)
Бот для записи в салон красоты с базовым функционалом:
- Меню на 3 языках (русский, английский, сербский)
- Хранение записей в памяти (без БД)
- Логирование в `bot.log`




> **Примечание**: Эта версия содержит ниже уязвимости (токен в коде) и устаревшую архитектуру. Актуальная версия — в ветке `refactored`.
__________________________________________________________
Описание проекта: Чат-бот для салона красоты Style Salon
1. Общая информация о проекте
Название проекта: StyleSalonBot

Тип проекта: Чат-бот для Telegram

Цель проекта: Автоматизация записи клиентов в салон красоты, увеличение продаж через ИИ-рекомендации, упрощение взаимодействия с клиентами и владельцем салона.

Текущий статус: Бот полностью функционален, поддерживает базовые функции (запись, просмотр записей, акции, отзывы, контакт с владельцем) и локализацию на трёх языках (сербский, русский, английский). Подготовлен для дальнейшего расширения (например, добавления админки).

Технологии:

Язык программирования: JavaScript (Node.js)
Библиотека для Telegram: node-telegram-bot-api
Среда выполнения: Node.js (версия 14.x или выше)
Хранение данных: В памяти (объекты JavaScript), с возможностью перехода на базу данных в будущем.
Логирование: Файл bot.log для отладки.
Целевая аудитория:

Клиенты салона красоты в Сербии (поддержка сербского, русского и английского языков).
Владелец салона (в будущем планируется админка для управления записями и расписанием).
Основные преимущества:

Автоматизация записи, что экономит время администратора салона.
ИИ-рекомендации для увеличения среднего чека (например, после стрижки предлагается укладка).
Многоязычность для привлечения клиентов из разных стран.
Возможность интеграции с Google Maps для отзывов.


2. Структура проекта
Проект организован в виде модульной структуры для удобства поддержки и расширения. Все файлы находятся в директории StyleSalonBot.

Структура директорий и файлов
text

Свернуть

Перенос

Копировать
StyleSalonBot/
├── data/                    # Данные для бота (услуги, акции, расписание)
│   ├── services.js          # Список услуг с ценами и описаниями
│   ├── promos.js            # Список акций
│   └── schedule.js          # Доступное время для записи
├── handlers/                # Обработчики команд и логики
│   ├── start.js             # Обработчик команды /start и главного меню
│   ├── book.js              # Логика записи (выбор услуги, времени, комментария)
│   ├── price.js             # Отображение прайс-листа
│   ├── about.js             # Информация о салоне
│   ├── promo.js             # Отображение акций
│   └── ai.js                # ИИ-рекомендации
├── index.js                 # Главный файл бота (логика обработки сообщений)
├── bot.log                  # Лог-файл для отладки
├── package.json             # Зависимости и скрипты
└── README.md                # Документация (рекомендуется создать)
Описание файлов
index.js: Главный файл, который запускает бота, обрабатывает входящие сообщения и распределяет команды по обработчикам. Содержит объект messages с локализованными текстами на трёх языках.
data/services.js: Хранит список услуг (стрижка, окрашивание, укладка) с ценами и описаниями на трёх языках.
data/promos.js: Хранит список акций (например, скидка 20% на окрашивание) с текстом на трёх языках.
data/schedule.js: Содержит доступное время для записи (например, "10:00 2023-10-01").
handlers/start.js: Отображает главное меню с кнопками.
handlers/book.js: Обрабатывает процесс записи: выбор услуги, времени, добавление комментария, сохранение записи.
handlers/price.js: Показывает прайс-лист.
handlers/about.js: Отображает информацию о салоне.
handlers/promo.js: Показывает текущие акции.
handlers/ai.js: Генерирует ИИ-рекомендации на основе предпочтений пользователя.
bot.log: Лог-файл, куда записываются действия бота и пользователя для отладки.



3. Текущий функционал
Основные функции
Выбор языка:
При запуске (/start) бот предлагает выбрать язык: сербский, русский или английский.
Пользователь может сменить язык в любой момент через кнопку "Сменить язык".
Все сообщения, названия услуг и акции отображаются на выбранном языке.
Запись на услугу:
Пользователь может выбрать услугу (стрижка, окрашивание, укладка).
Выбрать доступное время из списка.
Добавить комментарий к записи (например, "Используйте гипоаллергенную краску").
Запись сохраняется в памяти (bookings).
Проверка и отмена записей:
Пользователь может просмотреть свои записи через кнопку "Проверить мои записи".
Если записи есть, можно отменить любую из них через кнопку "Отменить запись".
Прайс-лист:
Показывает список услуг с ценами и описаниями на языке пользователя.
Акции:
Отображает текущие акции (например, скидка 20% на окрашивание) с пересчётом цены.
Информация о салоне:
Показывает адрес, часы работы, телефон и описание салона.
Связь с владельцем:
Пользователь может получить контактные данные владельца (телефон, email) через кнопку "Связаться с владельцем".
Оставить отзыв:
Пользователь может написать текстовый отзыв, который сохраняется в памяти (reviews).
После отзыва бот предлагает оставить отзыв на Google Maps (через ссылку).
ИИ-рекомендации:
После записи бот предлагает дополнительные услуги (например, после стрижки — укладку).
Рекомендации отображаются на языке пользователя.
Технические особенности
Локализация: Все тексты хранятся в объекте messages в index.js. Функция getMessage выбирает текст на основе языка пользователя.
Хранение данных:
Записи (bookings), отзывы (reviews) и языковые настройки (userLanguages) хранятся в памяти (объекты JavaScript).
Это временное решение, в будущем рекомендуется перейти на базу данных (например, SQLite или MongoDB).
Логирование: Все действия бота и пользователя записываются в bot.log для отладки.
Фильтрация занятого времени: При выборе времени бот исключает уже занятые слоты (на основе bookings).





4. Архитектура и зависимости
Архитектура
Бот построен по модульному принципу:

Входная точка (index.js): Обрабатывает все входящие сообщения, определяет текущий шаг пользователя (userState) и передаёт управление соответствующему обработчику.
Обработчики (handlers/): Каждый обработчик отвечает за свою функцию (например, book.js — за запись, price.js — за прайс-лист).
Данные (data/): Хранят статические данные (услуги, акции, расписание) в виде JavaScript-модулей.
Состояние:
userState хранит текущий шаг пользователя (например, selecting_service, selecting_time).
userPreferences хранит историю выбора услуг для ИИ-рекомендаций.
bookings и reviews хранят записи и отзывы.
userLanguages хранит выбранный язык пользователя.
Зависимости
Проект использует следующие зависимости (указаны в package.json):

json

Свернуть

Перенос

Копировать
{
  "name": "stylesalonbot",
  "version": "1.0.0",
  "description": "Telegram bot for Style Salon",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "node-telegram-bot-api": "^0.66.0"
  }
}
node-telegram-bot-api: Библиотека для взаимодействия с Telegram API. Используется в режиме polling (опрос сервера Telegram на новые сообщения).
Node.js: Требуется версия 14.x или выше.
Установка зависимостей
Если package.json уже есть, выполните:

bash

Свернуть

Перенос

Копировать
npm install
Если package.json отсутствует, создайте его с помощью:

bash

Свернуть

Перенос

Копировать
npm init -y
npm install node-telegram-bot-api


▎5. Инструкции по запуску

▎Требования

• Node.js: Установите версию 14.x или выше.

• Telegram Bot Token: Получите токен у BotFather в Telegram.

• ngrok (опционально): Для локального тестирования, если вы хотите использовать вебхуки вместо polling.

▎Шаги для запуска

1. Клонируйте проект (если он в репозитории):
      git clone <repository-url>
   cd StyleSalonBot
   

2. Установите зависимости:
      npm install
   

3. Настройте токен:

   • Откройте index.js.

   • Найдите строку const token = 'YOUR_TELEGRAM_TOKEN_HERE';.

   • Замените YOUR_TELEGRAM_TOKEN_HERE на ваш токен от BotFather.

4. Запустите ngrok (опционально, если хотите использовать вебхуки):
      cd "D:\проекты ЧАТ БОТЫ"
   .\ngrok.exe http 3000
   

   • Скопируйте URL (например, https://abc123.ngrok.io) и настройте вебхук через BotFather, если используете вебхуки. В текущей версии используется polling, так что это не обязательно.

5. Запустите бота:
      cd "D:\проекты ЧАТ БОТЫStyleSalonBot"
   node index.js
   

6. Проверьте работу:

   • Откройте Telegram, найдите ваш бот (например, @StyleSalonBot).

   • Напишите /start и следуйте инструкциям.

▎Логирование

Все действия бота и пользователя записываются в bot.log. Если возникают ошибки, проверьте этот файл для отладки.


▎6. Текущие ограничения

Хранение данных в памяти:  
Записи, отзывы и языковые настройки хранятся в памяти (объекты bookings, reviews, userLanguages). При перезапуске бота все данные теряются.  
Рекомендация: Перейти на базу данных (например, SQLite для простоты или MongoDB для масштабируемости).

Отсутствие админки:  
Владелец салона не может управлять расписанием, просматривать записи или отмечать занятые слоты.  
Рекомендация: Добавить админку (см. раздел "Дальнейшее развитие").

Ограниченная интеграция с Google Maps:  
Отзывы можно оставить только через ссылку на Google Maps. Полная интеграция с Google Maps API требует API-ключа и платного аккаунта.  
Рекомендация: Оставить как есть или интегрировать API, если бюджет позволяет.

Текстовые кнопки:  
Бот использует текстовые клавиатуры (reply_markup), которые менее удобны, чем интерактивные кнопки (inline_keyboard).  
Рекомендация: Перейти на интерактивные кнопки.

Отсутствие валидации времени:  
Расписание (schedule.js) статическое и не учитывает реальные рабочие часы салона.  
Рекомендация: Добавить динамическое расписание через админку.#   S t y l e   S a l o n   B o t  
 